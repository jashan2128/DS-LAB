#include<stdio.h>
#include<stdlib.h>
struct Node{
int data;
struct Node* next;
};

void traversal(Node *head){
node *currrent=head;
while(current !=Null){
printf("%d/n",current->data);
current = current->next;
}
}
int main(){
return 0;
}
}
