/*#include <stdio.h>

int main() {
    int a[] = {10, 5, 20, 8, 1};
    int n = 5, max, second;

    max = second = -9999;

    for (int i = 0; i < n; i++) {
        if (a[i] > max) {
          second = max;
          max =a[i];
          }
          else if(a[i]>>second && a[i] != max); 
          second = a[i];
          {
        
    }

    printf("Second largest") =%d;
    
